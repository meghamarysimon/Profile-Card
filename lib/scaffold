Scaffold(
          backgroundColor: Color.fromARGB(255, 170, 94, 200),
          body: SafeArea(
            child: Center(
              child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  const CircleAvatar(
                    radius: 90,
                    backgroundImage: AssetImage('asset/image/bbbbbb.jpeg'),
                  ),
                  const Text(
                    'MEGHA MARY SIMON',
                    style: TextStyle(
                        color: Colors.white,
                        fontSize: 40,
                        fontWeight: FontWeight.w500),
                  ),
                  Text(
                    'STUDENT'.toUpperCase(),
                    style: const TextStyle(
                        color: Colors.white70,
                        fontSize: 25,
                        letterSpacing: 2,
                        fontWeight: FontWeight.w300),
                  ),
                  const SizedBox(
                    width: 150,
                    child: Divider(
                      height: 10,
                      thickness: 1,
                      color: Colors.white54,
                    ),
                  ),
                  const Card(
                    elevation: 8,
                    color: Colors.white,
                    margin: EdgeInsets.symmetric(vertical: 10, horizontal: 30),
                    child: ListTile(
                      leading: Icon(
                        Icons.calendar_month,
                        color: Colors.teal,
                      ),
                      title: Text(
                        '31/12/2003',
                        style: TextStyle(color: Colors.teal),
                      ),
                    ),
                  ),
                  const Card(
                    elevation: 8,
                    color: Colors.white,
                    margin: EdgeInsets.symmetric(vertical: 10, horizontal: 30),
                    child: ListTile(
                      leading: Icon(
                        Icons.phone,
                        color: Colors.teal,
                      ),
                      title: Text(
                        '+91 8848802848',
                        style: TextStyle(color: Colors.teal),
                      ),
                    ),
                  ),
                  const Card(
                    elevation: 8,
                    color: Colors.white,
                    margin: EdgeInsets.symmetric(vertical: 10, horizontal: 30),
                    child: ListTile(
                      leading: Icon(
                        Icons.school,
                        color: Colors.teal,
                      ),
                      title: Text(
                        'Nirmala Bhavan\nMarian Enginnering College',
                        style: TextStyle(color: Colors.teal),
                      ),
                    ),
                  ),
                  const Card(
                    elevation: 8,
                    color: Colors.white,
                    margin: EdgeInsets.symmetric(vertical: 10, horizontal: 30),
                    child: ListTile(
                      leading: Icon(
                        Icons.sports_gymnastics,
                        color: Colors.teal,
                      ),
                      title: Text(
                        'Dancing\Cooking\ Traveling',
                        style: TextStyle(color: Colors.teal),
                      ),
                    ),
                  ),
                  const Card(
                    elevation: 8,
                    color: Colors.white,
                    margin: EdgeInsets.symmetric(vertical: 10, horizontal: 30),
                    child: ListTile(
                      leading: Icon(
                        Icons.location_city,
                        color: Colors.teal,
                      ),
                      title: Text(
                        'Trivandrum',
                        style: TextStyle(color: Colors.teal),
                      ),
                    ),
                  ),
                ],
              ),
            ),
          )),
    );
  }
}
